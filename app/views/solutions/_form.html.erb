<%= form_for(@solution) do |f| %>
  <% if @solution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@solution.errors.count, "error") %> prohibited this solution from being saved:</h2>

      <ul>
      <% @solution.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :case_id, :value => params[:case] %>
    <%= f.hidden_field :user_id,:value => current_user.id %>
  </div>
  <div class="field">
    <%= f.label :reason %><br />
    <%= f.text_area :reason,:cols => 60, :rows => 15 %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content,:cols => 60, :rows => 15 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
